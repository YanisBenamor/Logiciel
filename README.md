# Gestion des données projets

Application web CRUD complète pour un système de suivi de projets d'entreprise.

## Lancement

```bash
npm install
npm start
```

Puis ouvrir http://localhost:3000.

La base SQLite locale est créée dans `database.db` au premier lancement, avec le schéma et les données de départ fournis.